<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.movietime.dao.ShowDao">
    <!--<resultMap id="ShowResult" type="Show">-->
    <!--<id column="id" property="id" />-->
    <!--<result column="theater_name" property="theaterName" />-->
    <!--<result column="time" property="time" />-->
    <!--<result column="price" property="price" />-->
    <!--<result column="movie_id" property="movieId" />-->
    <!--<result column="seat_map" property="seatMap" />-->
    <!--</resultMap>-->

    <select id="findOne" resultType="Show">
        SELECT * FROM `show`
        WHERE id = #{id}
    </select>

    <select id="findAll" resultType="Show">
        SELECT * FROM `show`
    </select>

    <!--<select id="findByMovie" resultMap="ShowResult">-->
    <!--SELECT * FROM show-->
    <!--WHERE movie_id = #{movieId}-->
    <!--</select>-->

    <select id="findByMovie" resultType="Show">
        SELECT * FROM `show`
        WHERE movie_id = #{movieId}
    </select>
</mapper>